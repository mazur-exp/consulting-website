node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Секреты. Ключи для замера AI Visibility лежат в .secrets/aivis.env —
# в репозитории по пути, но НИКОГДА не в git. Не убирать эти строки.
.secrets/
*.env
.env
.env.*
